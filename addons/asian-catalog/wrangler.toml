name = "asian-catalog"
main = "worker.js"
compatibility_date = "2024-11-01"

# Optional configuration — uncomment / edit as needed.
# [vars]
# PINOYHUB_SITE = "https://pinoymovieshub.win"
# PINOYHUB_PAGE_LIMIT = "20"
# PINOYHUB_KEEP_UNMATCHED = "0"
#
# Secrets (better via `npx wrangler secret put TMDB_API_KEY`):
# TMDB_API_KEY = ""
